#include <iostream>
#include <iomanip>

using namespace std;

int main()
{
	char list;
	int n, sum=0;
	cin >> n;
	for (int i = 1; i <= n; i++)
	{
		cin >> list;
		sum += (list-'0');
	}
	cout << sum;
}
