n=int(input())
#dp(동적계획법)을 이용하기 위해 n개의 리스트 생성 1_index 이용
d=[0]*(n+1)

for i in range(2,n+1):
	d[i]=d[i-1]+1   #1을 빼는 경우
	if i%3==0:      #3으로 나눠지는 경우
		#-1과 3으로 나눈 것 중에 최소 연산 찾기 
		#+1 은 계산횟수 +1을 의미
		d[i]=min(d[i],d[i//3]+1)   
	#if elif을 안하는 이유 : 3으로 나눈 것 보다 2로 나눈게 더 최소 일 수 있어서
	#3으로 나눴을 때 하고 다시 최솟값 비교 하기 위해서
	if i%2==0:
		d[i]=min(d[i],d[i//2]+1)
print(d[n])
