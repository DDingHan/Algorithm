#include <iostream>
#include <string>

using namespace std;

int main()
{
	string str1{}, str2{};
	cin >> str1 >> str2;
	int count1 = 0, count2 = 0;
	for (int i = 0; i < str1.size() - 1; i++) ++count1;
	for (int i = 0; i < str2.size() - 1; i++) ++count2;
	if (count1 >= count2) cout << "go";
	else cout << "no";
}
